// export default function AddValueButton(props) {
//     return (
//         <>
//             <button onClick={() => props.onClick(props.count)}>{props.name}</button>
//         </>
//     )
// }

// export default function AddValueButton(props) {
//     // let calculatedValue = props.count
//     // calculatedValue = props.name === "Add 1" ? calculatedValue + 1 : calculatedValue - 1;
//
//     return (
//         <>
//             {props.name === "Add 1" ?
//                 <button onClick={() => {
//                     props.setCount(props.count + 1)
//                 }}>{props.name}</button>
//                 :
//                 <button onClick={() => {props.setCount(props.count - 1)}}>{props.name}</button>
//             }
//
//             {/*<button onClick={() => {*/}
//             {/*    props.setCount(calculatedValue)}}>{props.name}</button>*/}
//
//         </>
//     )
// }

